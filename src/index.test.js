import { CodeAreaShape } from '.'

describe('CodeAreaMap', () => {
  it('is truthy', () => {
    expect(CodeAreaMap).toBeTruthy()
  })
})

describe('CodeAreaShape', () => {
  it('is truthy', () => {
    expect(CodeAreaShape).toBeTruthy()
  })
})

describe('CodeMap', () => {
  it('is truthy', () => {
    expect(CodeMap).toBeTruthy()
  })
})

describe('CodeShape', () => {
  it('is truthy', () => {
    expect(CodeShape).toBeTruthy()
  })
})
